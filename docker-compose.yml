version: '3.8'

services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_GOOGLE_CLIENT_ID: ${NEXT_GOOGLE_CLIENT_ID}
    ports:
      - "3000:3000"
    env_file:
      - .env.local